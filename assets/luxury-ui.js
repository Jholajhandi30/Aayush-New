(()=>{const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches;document.body.classList.add('lux-page-enter');const reveal=[...document.querySelectorAll('section,.card,.production,.shot,.room-card,.look,.reel,.step')];reveal.forEach(el=>el.classList.add('lux-reveal'));if(!reduce&&'IntersectionObserver'in window){const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add('lux-in');io.unobserve(e.target)}}),{threshold:.12,rootMargin:'0px 0px -6%'});reveal.forEach(el=>io.observe(el))}else reveal.forEach(el=>el.classList.add('lux-in'));const tilt=[...document.querySelectorAll('.production,.card,.shot,.room-card,.look,.reel')];tilt.forEach(el=>{el.classList.add('lux-tilt','lux-depth');if(reduce)return;el.addEventListener('pointermove',e=>{if(innerWidth<901)return;const r=el.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;el.style.transform=`perspective(900px) rotateX(${(-y*4).toFixed(2)}deg) rotateY(${(x*5).toFixed(2)}deg) translateY(-3px)`});el.addEventListener('pointerleave',()=>el.style.transform='')});document.querySelectorAll('.btn,.navcta,.tab,.back').forEach(btn=>{btn.classList.add('lux-magnetic');if(reduce)return;btn.addEventListener('pointermove',e=>{if(innerWidth<901)return;const r=btn.getBoundingClientRect(),x=(e.clientX-r.left-r.width/2)*.12,y=(e.clientY-r.top-r.height/2)*.12;btn.style.transform=`translate(${x}px,${y}px)`});btn.addEventListener('pointerleave',()=>btn.style.transform='')});if(!reduce&&innerWidth>900){const glow=document.createElement('div');glow.className='lux-cursor-glow';document.body.appendChild(glow);window.addEventListener('pointermove',e=>{glow.style.left=e.clientX+'px';glow.style.top=e.clientY+'px';glow.classList.add('on')},{passive:true});document.documentElement.addEventListener('mouseleave',()=>glow.classList.remove('on'))}const hero=document.querySelector('.hero-bg,.hero:before');if(!reduce&&hero&&hero instanceof HTMLElement){window.addEventListener('scroll',()=>{const y=Math.min(scrollY*.06,24);hero.style.transform=`translateY(${y}px) scale(1.03)`},{passive:true})}})();